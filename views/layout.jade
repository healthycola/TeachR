doctype
html
    .wrap
        include inc/header
        body
            include inc/navigation
            .container-fluid-nopad
                block nopad-content
            .container
                block navigation
                - if (message && message != '')
                    .alert.alert-warning #{message}
                block content
            block endMainParent
    include inc/footer