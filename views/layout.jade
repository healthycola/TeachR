doctype
html
    include inc/header
    body
        include inc/navigation
        .container-fluid-nopad
            block nopad-content
        .container-fluid
            - if (message && message != '')
                .alert.alert-warning #{message}
            block content
        include inc/footer